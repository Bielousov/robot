dotenv
joblib
luma.core
luma.led_matrix
numpy==2.3.4
ollama
onnxruntime
psutil
python-dotenv
scikit-learn==1.9.0
silero-vad
sounddevice
spidev

# Model Training - CPU (PyTorch + LoRA fine-tuning for personality adapter)
torch; sys_platform == "darwin"
peft; sys_platform == "darwin"
transformers; sys_platform == "darwin"
datasets; sys_platform == "darwin"
huggingface_hub; sys_platform == "darwin"

# Model Training - GPU (MLX on Apple Silicon Macs only)
mlx; sys_platform == "darwin"
mlx-lm; sys_platform == "darwin"

# Hailo's GenAI runtime (provides hailo_platform module)
# Built for aarch64 only (RPi5/ARM), requires matching Python version (cp313 = Python 3.13)
# If hailo_platform import fails, check: python --version must match wheel (cp313/3.13, cp312/3.12, cp311/3.11)
./system/wheels/hailort-5.3.0-cp313-cp313-linux_aarch64.whl; platform_machine == "aarch64"
